# Schemas

The `schemas/` directory is reserved for future validation schemas.

No schema format is currently defined. Future schemas should validate documented inputs without triggering deployment behavior.
