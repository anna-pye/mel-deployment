# Security Model

This document will describe the security model for future deployment work.

The bootstrap repository does not access servers, manage credentials, open network connections, or define privilege boundaries. Those concerns must be documented before implementation begins.
